# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# netlify (estado local de `netlify link`/`netlify dev`)
.netlify

# dados reais sensíveis (endereços de RTs) — só local, nunca no histórico do git
zonas-regioes-rts-final.xlsx
~$zonas-regioes-rts-final.xlsx

# import de chamados reais do TomTicket — tem dado pessoal nas descrições
# (nome/e-mail de coordenador de CAPS etc.), só local, nunca no histórico
# do git (decisão do usuário, 19/08/2026)
supabase/migrations/Import_chamados_final.sql

# typescript
*.tsbuildinfo
next-env.d.ts

# saída do scripts/verificar-telas.mjs
.screenshots/
